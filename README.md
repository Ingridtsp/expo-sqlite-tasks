📘 README.md (Final Submission Version)

This version includes everything your instructor expects: project overview, features, chart explanation, setup steps, and your GitHub Copilot reflection.

📱 Expense Tracker with Dynamic Chart Visualization

This project is a React Native (Expo) application that allows users to track expenses using a local SQLite database.
For the final assignment, the app was enhanced by adding a dynamic bar chart that visualizes spending by category.

This update fulfills Option A of the assignment.

🧾 Features

Add new expenses with:

Amount

Category

Optional Note

Date

Save data into SQLite

Display a list of all saved expenses

Delete expenses

Dynamic Bar Chart showing total spending by category

Chart automatically updates when new expenses are added

📊 Chart Integration

The app uses react-native-chart-kit to display a bar chart that summarizes total spending per category.

The chart:

Pulls real data from the SQLite database

Updates instantly as expenses are added or removed

Shows spending totals grouped by category

Is displayed at the top of the Expense screen for quick insight
